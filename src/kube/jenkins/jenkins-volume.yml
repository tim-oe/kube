---
apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "jenkins-pvc"
  namespace: "jenkins"
  labels:
    app: "jenkins"
spec:
  storageClassName: "nfs"
  accessModes:
    - "ReadWriteMany"
  resources:
    requests:
      storage: "16Gi"
